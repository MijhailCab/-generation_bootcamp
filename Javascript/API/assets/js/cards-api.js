const colores = "https://jsonplaceholder.typicode.com/albums/1/photos"

const lista = document.querySelector("card-body")

fetch(colores).then(resp => resp.json()).then(datos =>{
    console.log(datos.message.id"3"); //Para verlos en general a la consola

    const arregloTiposColores = datos.message.terrier
    console.log(arregloTiposTerrier);

    arregloTiposColores.forEach(element => {
        console.log(tipo id 3);
        const template = ` `

    });
})

